import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: 'white',
    borderColor: 'none',
    width: '80%',
    height: 40,
    padding: 10,
    marginBottom: 10,
    marginHorizontal: 10,
    borderRadius: 4,
  },
});
