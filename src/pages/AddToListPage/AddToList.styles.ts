import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  label: {
    color: 'white',
    margin: 20,
    marginLeft: 0,
  },
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  categoryBlock: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
});
